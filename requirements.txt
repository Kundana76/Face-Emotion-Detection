# FEIS — Face Emotion Intelligence System
# Install: pip install -r requirements.txt

# ── Web Framework ───────────────────────────────────
flask>=2.3.0
flask-cors>=4.0.0

# ── Computer Vision ─────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# ── Emotion Detection (PRIMARY — installs TensorFlow) ──
fer>=22.5.1

# ── Emotion Detection (ALTERNATIVE — uncomment if FER fails) ──
# deepface>=0.0.79

# ── PDF Reports ─────────────────────────────────────
reportlab>=4.0.0

# ── Data & Logging ──────────────────────────────────
pandas>=2.0.0
python-dotenv>=1.0.0

# ── TF dependency (auto-installed by fer, pinned for stability) ──
# tensorflow>=2.12.0   # uncomment to pin version if needed
